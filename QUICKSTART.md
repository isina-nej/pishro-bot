# 🚀 راهنمای شروع سریع - Quick Start Guide

## ⚡ شروع سریع (۵ دقیقه)

### مرحله ۱: کپی فایل تنظیمات
```bash
cp .env.example .env
```

### مرحله ۲: دریافت Telegram Bot Token
1. در تلگرام، `@BotFather` را پیدا کنید
2. دستور `/newbot` بزنید
3. نام و یوزرنیم بات خود را وارد کنید
4. توکن تولید شده را کپی کنید

### مرحله ۳: تنظیم .env
```env
BOT_TOKEN=توکن_خود_را_اینجا_جایگذاری_کنید
DATABASE_URL=postgresql+asyncpg://pishro_user:pishro_password@localhost:5432/investment_bot
ADMIN_TELEGRAM_IDS=شناسه_تلگرام_خود
ACCOUNTANT_TELEGRAM_IDS=921234567,987654321
```

### مرحله ۴: Redis و PostgreSQL
```bash
# اگر Docker دارید:
docker-compose up -d

# یا نصب محلی:
# PostgreSQL: https://www.postgresql.org/download/
```

### مرحله ۵: اجرای ربات
```bash
# نصب وابستگی‌ها
pip install -r requirements.txt

# مقدار‌دهی دیتابیس
python init_db.py

# اجرای ربات
python run_bot.py
```

## ✅ تست سریع

### ۱. تست احراز هویت
```
شناسه تلگرام: 123456789
شماره تماس: 09121234567
نام: دکتر ایرج (مدیر)
```

### ۲. تست سرمایه‌گذار
```
شناسه تلگرام: 111111111
شماره تماس: 09121111111
نام: احمد علی
```

### ۳. تست حسابدار
```
شناسه تلگرام: 987654321
شماره تماس: 09129876543
نام: حسابدار شرکت
```

## 📊 ساختار فایل‌ها

```
pishro-bot/
├── app/
│   ├── bot.py                      # نقطه ورود اصلی
│   ├── config.py                   # تنظیمات
│   ├── middleware.py               # Middleware
│   ├── models/models.py            # مدل‌های دیتابیسی
│   ├── handlers/
│   │   ├── auth.py                 # احراز هویت
│   │   ├── investor.py             # سرمایه‌گذاران
│   │   ├── accountant.py           # حسابداران
│   │   └── admin.py                # ادمین‌ها
│   ├── services/                   # منطق تجاری
│   ├── keyboards/inline.py         # دکمه‌های Inline
│   └── utils/
│       ├── formatters.py           # فرمتینگ و تبدیل‌ها
│       └── logger.py               # لاگینگ
├── init_db.py                      # بارگذاری دیتابیس
├── run_bot.py                      # اجرای ربات
├── requirements.txt                # وابستگی‌ها
├── Dockerfile                      # Docker
├── docker-compose.yml              # Docker Compose
└── README.md                       # مستندات
```

## 🐛 حل مشکلات رایج

### خطا: "Module not found"
```bash
pip install -r requirements.txt
```

### خطا: "DATABASE_URL is required"
```bash
# بررسی کنید .env موجود است
cat .env

# درغیر این صورت دوباره کپی کنید
cp .env.example .env
```

### خطا: "can't connect to database"
```bash
# بررسی PostgreSQL
docker ps
# یا
psql -U pishro_user -d investment_bot
```

### ربات پاسخ نمی‌دهد
```bash
# بررسی توکن
echo $BOT_TOKEN

# بررسی لاگ‌ها
tail -f logs/bot.log

# بررسی firewall
```

## 🔐 بهترین روش‌های امنیتی

1. **هرگز توکن را publik نکنید** ❌
2. **از .env استفاده کنید** ✅
3. **دیتابیس را محافظت کنید** ✅
4. **لاگ‌ها را مانیتور کنید** ✅
5. **نسخه‌های پشتیبان بگیرید** ✅

## 📚 مراجع مفید

- [راهنمای Aiogram](https://docs.aiogram.dev/)
- [مستندات SQLAlchemy](https://docs.sqlalchemy.org/)
- [APIتلگرام](https://core.telegram.org/bots/api)
- [PostgreSQL Docs](https://www.postgresql.org/docs/)

## 💡 نکات مهم

### تاریخ‌های شمسی
تمام تاریخ‌ها به‌صورت خودکار به فرمت شمسی تبدیل می‌شوند:
```python
from app.utils.formatters import format_jalali_date
format_jalali_date(date.today())  # "فروردین 10, 1402"
```

### فرمتینگ پول
```python
from app.utils.formatters import format_currency
format_currency(1_000_000_000)  # "1,000,000,000 تومان"
```

### دسترسی‌های مختلف
```
سرمایه‌گذار: 👤 فقط مشاهده
حسابدار:    💼 ثبت و ویرایش
ادمین:       👨‍💼 کامل
```

## 🚀 مراحل بعدی

1. ✅ درک ساختار پروژه
2. ✅ اجرا و تست
3. ⬜ سفارشی‌سازی
4. ⬜ اضافه کردن ویژگی‌های جدید
5. ⬜ استقرار (Deployment)

## 📞 پشتیبانی

مشکلی دارید؟
- 📧 support@pishro.dev
- 💬 @pishro_support
- 🐛 Issues: GitHub Issues

---

**نسخه**: 1.0.0 | **تاریخ**: فروردین ۱۴۰۲ | **وضعیت**: ✅ آماده
